<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>채팅 서버 로그</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
        }
        .logs-container {
            max-width: 800px;
            margin: auto;
        }
        .log {
            margin-bottom: 10px;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        .log.error {
            background-color: #fdd;
        }
        .log.info {
            background-color: #e0f7fa;
        }
        .log.success {
            background-color: #e8f5e9;
        }
    </style>
</head>
<body>
    <div class="logs-container">
        <h1>채팅 서버 로그</h1>
        <form method="GET" action="/logs">
            <input type="text" name="search" value="<%= search %>" placeholder="검색어 입력" />
            <button type="submit">검색</button>
        </form>
        <hr />
        <% logs.forEach(log => { %>
            <div class="log <%= log.type %>">
                <strong>타입:</strong> <%= log.type %><br />
                <strong>사용자:</strong> <%= log.user || 'N/A' %><br />
                <strong>내용:</strong> <%= log.text || 'N/A' %><br />
                <strong>IP:</strong> <%= log.clientIP || 'N/A' %><br />
                <strong>시간:</strong> <%= log.timestamp.toLocaleString() %>
            </div>
        <% }); %>
    </div>
</body>
</html>
