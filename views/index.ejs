<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>홈</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/styles.css">
  <meta name="naver-site-verification" content="babf82f50e864df1554947c15bf1215983b38a6f" />
  <style>
    /* 기본 스타일 */
    body {
      font-family: Arial, sans-serif;
      background-color: #f8f9fa;
      color: #333;
      font-size: 16px; /* 기본 폰트 크기 */
    }

    main {
      background-color: #fff;
      padding: 5vw; /* 반응형 여백 */
      border-radius: 1vw; /* 반응형 모서리 둥글기 */
      box-shadow: 0 0.5vw 1vw rgba(0, 0, 0, 0.1); /* 반응형 그림자 */
      max-width: 90vw;
      width: 100%;
      margin: 10vh auto;
    }

    h1 {
      color: #343a40;
      font-size: 2.5rem; /* 기본 크기, 반응형 폰트 */
    }

    p.lead {
      font-size: 1.2rem; /* 기본 크기, 반응형 폰트 */
    }

    /* 미디어 쿼리 */
    @media (max-width: 768px) {
      body {
        font-size: 14px; /* 작은 화면에서 폰트 크기 축소 */
      }

      main {
        padding: 8vw;
        border-radius: 2vw;
        box-shadow: 0 0.8vw 1.6vw rgba(0, 0, 0, 0.1);
      }

      h1 {
        font-size: 2rem; /* 작은 화면에서 제목 크기 축소 */
      }

      p.lead {
        font-size: 1rem; /* 작은 화면에서 본문 크기 축소 */
      }
    }

    @media (max-width: 480px) {
      body {
        font-size: 12px; /* 더 작은 화면에서 폰트 크기 축소 */
      }

      main {
        padding: 10vw;
        border-radius: 3vw;
        box-shadow: 0 1vw 2vw rgba(0, 0, 0, 0.1);
      }

      h1 {
        font-size: 1.8rem; /* 더 작은 화면에서 제목 크기 축소 */
      }

      p.lead {
        font-size: 0.9rem; /* 더 작은 화면에서 본문 크기 축소 */
      }
    }
  </style>
</head>
<body>
  <!-- 헤더 포함 -->
  <%- include('partials/header') %>

  <main class="container text-center">
    <h1 class="mb-4">환영합니다!</h1>
    <% if (isAdmin) { %>
      <p class="alert alert-info">관리자가 로그인했습니다.</p>
    <% } %>
    <p class="lead">접속한 IP: <%= ip %></p>
    <p class="lead">국가: <%= country %></p>
    <p class="lead">지역: <%= region %></p>
  </main>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
